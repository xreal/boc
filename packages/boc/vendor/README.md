# Bergflow artifact

`bergflow-opencode-0.2.0-ae41c0880d38.tgz` is the private `@bergflow/opencode` 0.2.0 package built from the Bergflow repository at commit `1130767` with `bun run build` and `npm pack`.

SHA-256: `ae41c0880d3891e35028224c2739f1e09e401d73e38c0b534f85e2915dbe3d4e`.

The content-addressed filename prevents Bun from reusing stale local archive contents. Update this artifact and the package dependency/lockfile together, then rebuild and run the compatibility checks in `BOC_BERGFLOW.md`. The portable repository remains the source of truth; do not edit the archive by hand. No registry publication is implied.
